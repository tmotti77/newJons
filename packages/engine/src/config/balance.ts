/**
 * ALL tunable game numbers will live here (DEV_PLAN §3.2 / §8.1).
 * Populated in Phase 1 from the §2 tables (jobs, housing, goals, etc.).
 * Placeholder constant below only proves the package/test wiring works.
 */
export const TIME_UNITS_PER_WEEK = 60;
